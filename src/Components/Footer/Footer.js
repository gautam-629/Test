import React from 'react';

import { TopFooter, BottomFooter } from '../../Components';

const Footer = () => {
	return (
		<>
			<TopFooter />
			<BottomFooter />
		</>
	);
};

export default Footer;
